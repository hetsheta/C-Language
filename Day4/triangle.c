#include<stdio.h>

int main()
{
	int b,h;
	float a;
printf("Enter the base of the triangle:");
scanf("%d",&b);
printf("Enter the height of the triangle:");
scanf("%d",&h);

printf("Area of the triangle is %.2f",a=0.5*b*h);
return 0;

}
